perc<-full_join(burst, all)
perc$percent.bb<-perc$burst/perc$total
perc$species<-substr(perc$id, 1,6)
mod<-lm(percent.bb~tx + species, data=perc)
display(mod)
qplot(species, percent.bb, data = perc,
geom = "boxplot", color=tx) +
xlab("Species")+ylab("Percent Budburst")
dx$bbch
unique(dx$bbch)
xx<-subset(dx, bbch="dead")
unique(xx$NEW)
View(xx)
unique(xx$bbch)
phases<-c("15", "dead")
xx<-subset(xx, bbch %in% phases)
View(xx)
phase<-c("0", "dead", "x")
xx<-subset(dx, bbch %in% phase)
View(xx)
last<-aggregate(dx$doy.adjusted, by = list(dx$NEW, dx$bud), max)
View(last)
length(unique(last$Group.1))
last<-aggregate(dx$doy.adjusted, by = list(dx$NEW, dx$bud, dx$bbch), max)
View(last)
last<-last%>%rename(NEW=Group.1)%>%rename(doy.adjusted=x)
last<-aggregate(dx$doy.adjusted, by = list(dx$NEW, dx$bud), max)
last<-last%>%rename(NEW=Group.1)%>%rename(doy.adjusted=x)
View(last)
last<-cbind(last, dx$bbch)
View(last)
last$bbch<-NA
for(i in c(1:nrow(last))){
for(j in c(1:nrow(dx))){
if(last$NEW[i]=dx$NEW[j] & last$doy.adjusted[i]=dx$doy.adjusted[j]){
last$bbch[i]<-dx$bbch[j]
}
}
}
for(i in c(1:nrow(last))){
for(j in c(1:nrow(dx))){
if(last$NEW[i]=dx$NEW[j] & last$doy.adjusted[i]=dx$doy.adjusted[j])
last$bbch[i]<-dx$bbch[j]
}
}
for(i in c(1:nrow(last))){
for(j in c(1:nrow(dx)))
if(last$NEW[i]=dx$NEW[j] & last$doy.adjusted[i]=dx$doy.adjusted[j])
last$bbch[i]<-dx$bbch[j]
}
last$bbch<-NA
for(i in c(1:nrow(last))) {
for(j in c(1:nrow(dx)))
if(last$NEW[i]==dx$NEW[j] & last$doy.adjusted[i]==dx$doy.adjusted[j])
last$bbch[i]<-dx$bbch[j]
}
end<-dx[!is.na(dx$bbch),]
last<-aggregate(end$doy.adjusted, by = list(end$NEW, end$bud), max)
last<-last%>%rename(NEW=Group.1)%>%rename(doy.adjusted=x)
last$bbch<-NA
for(i in c(1:nrow(last))) {
for(j in c(1:nrow(end)))
if(last$NEW[i]==end$NEW[j] & last$doy.adjusted[i]==end$doy.adjusted[j])
last$bbch[i]<-end$bbch[j]
}
View(end)
class(last$doy.adjuste)
class(end$doy.adjuste)
class(end$doy.adjusted)
class(last$doy.adjusted)
class(dx$doy.adjusted)
last<-last%>%rename(NEW=Group.1)%>%rename(doy.adjusted=x)%>%rename(bud=Group.2)
end<-dx[!is.na(dx$bbch),]
end<-end%>%dplyr::select(NEW, doy.adjusted, bbch)
last<-aggregate(end$doy.adjusted, by = list(end$NEW, end$bud), max)
end<-dx[!is.na(dx$bbch),]
end<-end%>%dplyr::select(NEW, doy.adjusted, bbch, bud)
last<-aggregate(end$doy.adjusted, by = list(end$NEW, end$bud), max)
last<-last%>%rename(NEW=Group.1)%>%rename(doy.adjusted=x)%>%rename(bud=Group.2)
last$bud<-as.numeric(last$bud)
class(last$bud)
last$bbch<-NA
for(i in c(1:nrow(last))) {
for(j in c(1:nrow(end)))
if(last$NEW[i]==end$NEW[j] & last$doy.adjusted[i]==end$doy.adjusted[j] & last$bud[i]==end$bud[j])
last$bbch[i]<-end$bbch[j]
}
View(last)
View(end)
last<-full_join(last, end)
class(end$bud)
end<-dx[!is.na(dx$bbch),]
end<-end%>%dplyr::select(NEW, doy.adjusted, bbch, bud)
end$bud<-as.numeric(end$bud)
last<-aggregate(end$doy.adjusted, by = list(end$NEW, end$bud), max)
last<-last%>%rename(NEW=Group.1)%>%rename(doy.adjusted=x)%>%rename(bud=Group.2)
last$bbch<-NA
for(i in c(1:nrow(last))) {
for(j in c(1:nrow(end)))
if(last$NEW[i]==end$NEW[j] & last$doy.adjusted[i]==end$doy.adjusted[j] & last$bud[i]==end$bud[j])
last$bbch[i]<-end$bbch[j]
}
View(last)
unique(end$bbch)
class(end$bud)
class(last$bbch)
end<-dx[!is.na(dx$bbch),]
end<-end%>%dplyr::select(NEW, doy.adjusted, bbch, bud)
end$bud<-as.numeric(end$bud)
last<-aggregate(end$doy.adjusted, by = list(end$NEW, end$bud), max)
last<-last%>%rename(NEW=Group.1)%>%rename(doy.adjusted=x)%>%rename(bud=Group.2)
last$bbch<-NA
for(i in c(1:nrow(last))) {
for(j in c(1:nrow(end)))
last$bbch[i]<-ifelse(last$NEW[i]==end$NEW[j] & last$doy.adjusted[i]==end$doy.adjusted[j] & last$bud[i]==end$bud[j], end$bbch[j], last$bbch[i])
}
View(last)
end<-dx[!is.na(dx$bbch),]
end<-end%>%dplyr::select(NEW, doy.adjusted, bbch, bud)
end$bud<-as.numeric(end$bud)
last<-aggregate(end$doy.adjusted, by = list(end$NEW, end$bud), max)
last<-last%>%rename(NEW=Group.1)%>%rename(doy.adjusted=x)%>%rename(bud=Group.2)
last<-full_join(last, end)
View(last)
end<-dx[!is.na(dx$bbch),]
end<-end%>%dplyr::select(NEW, doy.adjusted, bbch, bud)
end$bud<-as.numeric(end$bud)
last<-aggregate(end$doy.adjusted, by = list(end$NEW, end$bud), max)
last<-last%>%rename(NEW=Group.1)%>%rename(doy.adjusted=x)%>%rename(bud=Group.2)
last<-right_join(last, end)
last$bbch <- end$bbch[match(last$NEW, end$NEW) & match(last$doy.adjusted, end$doy.adjusted)]
end<-dx[!is.na(dx$bbch),]
end<-dx[!is.na(dx$bbch),]
end<-dx[!is.na(dx$bbch),]
end<-dx[!is.na(dx$bbch),]
end<-end%>%dplyr::select(NEW, doy.adjusted, bbch, bud)
end$bud<-as.numeric(end$bud)
last<-aggregate(end$doy.adjusted, by = list(end$NEW, end$bud), max)
last<-last%>%rename(NEW=Group.1)%>%rename(doy.adjusted=x)%>%rename(bud=Group.2)
last$bbch <- end$bbch[match(last$NEW, end$NEW) & match(last$doy.adjusted, end$doy.adjusted)]
end<-dx[!is.na(dx$bbch),]
end<-end%>%dplyr::select(NEW, doy.adjusted, bbch, bud)
end$bud<-as.numeric(end$bud)
last<-aggregate(end$doy.adjusted, by = list(end$NEW, end$bud), max)
last<-last%>%rename(NEW=Group.1)%>%rename(doy.adjusted=x)%>%rename(bud=Group.2)
last<-left_join(last, end)
View(last)
end<-dx[!is.na(dx$bbch),]
end$bud<-as.numeric(end$bud)
end$ID<-paste(end$NEW, end$bud, sep="_")
end<-end%>%dplyr::select(NEW, doy.adjusted, bbch, bud)
end<-dx[!is.na(dx$bbch),]
end$bud<-as.numeric(end$bud)
end$ID<-paste(end$NEW, end$bud, sep="_")
end<-end%>%dplyr::select(ID, doy.adjusted, bbch)
last<-aggregate(end$doy.adjusted, by = list(end$NEW, end$bud), max)
last<-aggregate(end$doy.adjusted, by = list(end$ID), max)
last<-last%>%rename(ID=Group.1)%>%rename(bud=Group.2)
last<-last%>%rename(ID=Group.1)%>%rename(bbch=x)
last<-NA
for(i in c(1:nrow(last))) {
for(j in c(1:nrow(end)))
if(last$NEW[i]==end$NEW[j] & last$doy.adjusted[i]==end$doy.adjusted[j])
last$bbch[i]<-end$bbch[j]
}
end<-dx[!is.na(dx$bbch),]
end$bud<-as.numeric(end$bud)
end$ID<-paste(end$NEW, end$bud, sep="_")
end<-end%>%dplyr::select(ID, doy.adjusted, bbch)
last<-aggregate(end$doy.adjusted, by = list(end$ID), max)
last$bbch<-NA
for(i in c(1:nrow(last))) {
for(j in c(1:nrow(end)))
if(last$NEW[i]==end$NEW[j] & last$doy.adjusted[i]==end$doy.adjusted[j])
last$bbch[i]<-end$bbch[j]
}
for(i in c(1:nrow(last))) {
for(j in c(1:nrow(end)))
if(last$ID[i]==end$ID[j] & last$doy.adjusted[i]==end$doy.adjusted[j])
last$bbch[i]<-end$bbch[j]
}
View(last)
last<-last%>%rename(ID=Group.1)%>%rename(bbch=x)
for(i in c(1:nrow(last))) {
for(j in c(1:nrow(end)))
if(last$ID[i]==end$ID[j] & last$doy.adjusted[i]==end$doy.adjusted[j])
last$bbch[i]<-end$bbch[j]
}
end<-dx[!is.na(dx$bbch),]
end$bud<-as.numeric(end$bud)
end$ID<-paste(end$NEW, end$bud, sep="_")
end<-end%>%dplyr::select(ID, doy.adjusted, bbch)
last<-aggregate(end$doy.adjusted, by = list(end$ID), max)
last<-last%>%rename(ID=Group.1)%>%rename(doy.adjusted=x)
last$bbch<-NA
for(i in c(1:nrow(last))) {
for(j in c(1:nrow(end)))
if(last$ID[i]==end$ID[j] & last$doy.adjusted[i]==end$doy.adjusted[j])
last$bbch[i]<-end$bbch[j]
}
View(last)
end<-dx[!is.na(dx$bbch),]
end$bud<-as.numeric(end$bud)
end$ID<-paste(end$NEW, end$bud, sep="_")
end<-end%>%dplyr::select(ID, doy.adjusted, bbch)
last<-aggregate(end$doy.adjusted, by = list(end$ID[which(bbch>=0)]), max)
last<-aggregate(end$doy.adjusted, by = list(end$ID[which(end$bbch>=0)]), max)
end<-dx[!is.na(dx$bbch),]
end$bbch<-ifelse(end$bbch=="", NA, end$bbch)
end<-dx[!is.na(dx$bbch),]
end$bbch<-ifelse(end$bbch=="", NA, end$bbch)
end$bud<-as.numeric(end$bud)
View(end)
unique(end$bbch)
end<-dx[!is.na(dx$bbch),]
done<-c("DONE!", "DONE", "DONE!!", "missed","(missed")
end<-end[!end$bbch%in%done]
end<-end[!end$bbch%in%done,]
dead<-c("stem snapped", "x", "dead")
end$bbch<-ifelse(end$bbch%in%dead, 0, end$bbch)
unique(end$bbch)
end<-dx
end$bbch<-ifelse(end$bbch=="", NA, end$bbch)
end<-dx[!is.na(dx$bbch),]
done<-c("DONE!", "DONE", "DONE!!", "missed","(missed)", "")
end<-end[!end$bbch%in%done,]
dead<-c("stem snapped", "x", "dead")
end$bbch<-ifelse(end$bbch%in%dead, 0, end$bbch)
unique(end$bbch)
end$bud<-as.numeric(end$bud)
end$ID<-paste(end$NEW, end$bud, sep="_")
end<-end%>%dplyr::select(ID, doy.adjusted, bbch)
last<-aggregate(end$doy.adjusted, by = list(end$ID[which(end$bbch>=0)]), max)
last<-last%>%rename(ID=Group.1)%>%rename(doy.adjusted=x)
last$bbch<-NA
for(i in c(1:nrow(last))) {
for(j in c(1:nrow(end)))
if(last$ID[i]==end$ID[j] & last$doy.adjusted[i]==end$doy.adjusted[j])
last$bbch[i]<-end$bbch[j]
}
View(last)
start<-dx[!is.na(dx$bbch),]
unique(start$bbch)
dvr<-dx[!is.na(dx$bbch),]
done<-c("DONE!", "DONE", "DONE!!", "missed","(missed)", "")
dvr<-dvr[!dvr$bbch%in%done,]
dead<-c("stem snapped", "x", "dead")
dvr$bbch<-ifelse(dvr$bbch%in%dead, 0, dvr$bbch)
dvr$bud<-as.numeric(dvr$bud)
dvr$ID<-paste(dvr$NEW, dvr$bud, sep="_")
dvr<-dvr%>%dplyr::select(ID, doy.adjusted, bbch)
last<-aggregate(dvr$doy.adjusted, by = list(dvr$ID), max)
last<-last%>%rename(ID=Group.1)%>%rename(doy.adjusted=x)
last$leaf<-NA
for(i in c(1:nrow(last))) {
for(j in c(1:nrow(dvr)))
if(last$ID[i]==dvr$ID[j] & last$doy.adjusted[i]==dvr$doy.adjusted[j])
last$leaf[i]<-dvr$bbch[j]
}
first<-aggregate(dvr$doy.adjusted, by = list(dvr$ID), min)
first<-first%>%rename(ID=Group.1)%>%rename(doy.adjusted=x)
first$bb<-NA
for(i in c(1:nrow(first))) {
for(j in c(1:nrow(start)))
if(first$NEW[i]==start$NEW[j] & first$doy.adjusted[i]==start$doy.adjusted[j])
first$bb[i]<-start$bbch[j]
}
View(dvr)
View(first)
for(i in c(1:nrow(first))) {
for(j in c(1:nrow(start)))
if(first$ID[i]==start$ID[j] & first$doy.adjusted[i]==start$doy.adjusted[j])
first$bb[i]<-start$bbch[j]
}
for(i in c(1:nrow(first))) {
for(j in c(1:nrow(dvr)))
if(first$ID[i]==dvr$ID[j] & first$doy.adjusted[i]==dvr$doy.adjusted[j])
first$bb[i]<-dvr$bbch[j]
}
first<-first%>%rename(budburst=doy.adjusted)%>%rename(bbch=bb)
last<-last%>%rename(leafout=doy.adjusted)%>%rename(bbch=bb)
last<-last%>%rename(leafout=doy.adjusted)%>%rename(bbch=leaf)
first<-first%>%rename(budburst=doy.adjusted)%>%rename(bbch.first=bb)
last<-last%>%rename(leafout=doy.adjusted)%>%rename(bbch.last=leaf)
dvr<-dx[!is.na(dx$bbch),]
done<-c("DONE!", "DONE", "DONE!!", "missed","(missed)", "")
dvr<-dvr[!dvr$bbch%in%done,]
dead<-c("stem snapped", "x", "dead")
dvr$bbch<-ifelse(dvr$bbch%in%dead, 0, dvr$bbch)
dvr$bud<-as.numeric(dvr$bud)
dvr$ID<-paste(dvr$NEW, dvr$bud, sep="_")
dvr<-dvr%>%dplyr::select(ID, doy.adjusted, bbch)
last<-aggregate(dvr$doy.adjusted, by = list(dvr$ID), max)
last<-last%>%rename(ID=Group.1)%>%rename(doy.adjusted=x)
last$leaf<-NA
for(i in c(1:nrow(last))) {
for(j in c(1:nrow(dvr)))
if(last$ID[i]==dvr$ID[j] & last$doy.adjusted[i]==dvr$doy.adjusted[j])
last$leaf[i]<-dvr$bbch[j]
}
first<-aggregate(dvr$doy.adjusted, by = list(dvr$ID), min)
first<-first%>%rename(ID=Group.1)%>%rename(doy.adjusted=x)
first$bb<-NA
for(i in c(1:nrow(first))) {
for(j in c(1:nrow(dvr)))
if(first$ID[i]==dvr$ID[j] & first$doy.adjusted[i]==dvr$doy.adjusted[j])
first$bb[i]<-dvr$bbch[j]
}
first<-first%>%rename(budburst=doy.adjusted)%>%rename(bbch.first=bb)
last<-last%>%rename(leafout=doy.adjusted)%>%rename(bbch.last=leaf)
risk<-full_join(first, last)
risk$dvr<-risk$leafout - risk$budburst
View(risk)
risk$species<-substr(risk$ID, 1,6)
risk$individ<-substr(risk$ID, 1, 10)
risk$bud<-substr(risk$ID, 12, 13)
View(dx)
frz<-subset(dx,TX=="B")
frz<-dplyr::select(frz, NEW, Freeze, bud)
frz<-subset(dx,TX=="B")
frz<-dplyr::select(frz, NEW, Freeze, bud)
frz$ID<-paste(frz$NEW,frz$bud, sep="_")
View(frz)
frz$Freeze<-as.Date(frz$Freeze)
frz<-subset(dx,TX=="B")
frz<-dplyr::select(frz, NEW, Freeze, bud)
frz$ID<-paste(frz$NEW,frz$bud, sep="_")
frz$day<-substr(frz$Freeze, 1, 2)
frz$month<-substr(frz$Freeze, 4, 5)
frz$year<-substr(frz$Freeze, 7,10)
x<-paste(frz$year, frz$day, frz$month)
frz$date<-as.Date(strptime(x, format="%Y %d %m"))
frz$doy<-yday(frz$date)
frz$frz<-yday(frz$date)
frz<-dplyr::select(frz, ID, frz)
frz<-na.omit(frz)
frz<-frz[!duplicated(frz),]
View(frz)
frz<-subset(dx,TX=="B")
frz<-dplyr::select(frz, NEW, Freeze, bud)
frz$day<-substr(frz$Freeze, 1, 2)
frz$month<-substr(frz$Freeze, 4, 5)
frz$year<-substr(frz$Freeze, 7,10)
x<-paste(frz$year, frz$day, frz$month)
frz$date<-as.Date(strptime(x, format="%Y %d %m"))
frz$frz<-yday(frz$date)
frz<-dplyr::select(frz, NEW, frz)
frz<-na.omit(frz)
frz<-frz[!duplicated(frz),]
View(risk)
risk$frz<-NA
for(i in c(1:nrow(risk))) {
for(j in c(1:nrow(frz)))
if(risk$individ[i]==frz$NEW[j])
risk$frz[i]<-frz$frz[j]
}
risk$tx<-ifelse(is.na(risk$frz), "A", "B")
risk$dvr<-risk$leafout[which(risk$leafout==15)] - risk$budburst
risk$dvr<-ifelse(risk$leafout==15, (risk$leafout-risk$budburst), NA)
risk$dvr<-risk$leafout[which(risk$bbch.last==15)]-risk$budburst
risk$dvr<-ifelse(risk$bbch.last==15, (risk$leafout-risk$budburst), NA)
burst<-dx[!is.na(dx$dvr),]
burst<-dx
burst$dvr<-dx[!is.na(dx$dvr),]
burst<-risk[!is.na(risk$dvr),]
View(burst)
burst<-distinct(burst, ID,individ)
burst<-as.data.frame(table(burst$individ))
burst<-burst%>%rename(individ=Var1)%>%rename(burst=Freq)
class(burst$individ)
burst$id<-as.character(burst$id)
burst$individ<-as.character(burst$individ)
total<-distinct(total, ID,individ)
total<-as.data.frame(table(total$individ))
total<-risk
total<-distinct(total, ID,individ)
total<-as.data.frame(table(total$individ))
total<-total%>%rename(individ=Var1)%>%rename(total=Freq)
total$individ<-as.character(total$individ)
View(total)
percent<-full_join(total, burst)
percent$perc.bb<-percent$burst/percent$total
View(percent)
percent$tx<-NA
for(i in c(1:nrow(percent))){
for(j in c(1:nrow(risk)))
if(percent$individ[i]==risk$individ[j])
percent$tx[i]==risk$tx[j]
}
class(percent$individ)
class(risk$individ)
class(percent$tx)
class(risk$tx)
percent$tx<-NA
percent$tx<-as.character(percent$tx)
for(i in c(1:nrow(percent))){
for(j in c(1:nrow(risk)))
if(percent$individ[i]==risk$individ[j])
percent$tx[i]==risk$tx[j]
}
for(i in c(1:nrow(percent))){
for(j in c(1:nrow(risk)))
if(percent$individ[i]==risk$individ[j])
percent$tx[i]<-risk$tx[j]
}
mod<-lm(perc.bb~tx + species, data=percent)
percent$species<-substr(percent$individ, 1, 6)
mod<-lm(perc.bb~tx + species, data=percent)
display(mod)
qplot(species, percent.bb, data = perc,
geom = "boxplot", color=tx) +
xlab("Species")+ylab("Percent Budburst")
qplot(species, perc.bb, data = percent,
geom = "boxplot", color=tx) +
xlab("Species")+ylab("Percent Budburst")
leaf<-c(14, 15)
leaf<-c(14, 15)
risk$dvr<-ifelse(risk$bbch.last%in%leaf, (risk$leafout-risk$budburst), NA)
###### Re-evaluated % budburst #######
burst<-risk[!is.na(risk$dvr),]
burst<-distinct(burst, ID,individ)
burst<-as.data.frame(table(burst$individ))
burst<-burst%>%rename(individ=Var1)%>%rename(burst=Freq)
burst$individ<-as.character(burst$individ)
total<-risk
total<-distinct(total, ID,individ)
total<-as.data.frame(table(total$individ))
total<-total%>%rename(individ=Var1)%>%rename(total=Freq)
total$individ<-as.character(total$individ)
percent<-full_join(total, burst)
percent$perc.bb<-percent$burst/percent$total
percent$species<-substr(percent$individ, 1,6)
percent$tx<-NA
percent$tx<-as.character(percent$tx)
for(i in c(1:nrow(percent))){
for(j in c(1:nrow(risk)))
if(percent$individ[i]==risk$individ[j])
percent$tx[i]<-risk$tx[j]
}
percent$species<-substr(percent$individ, 1, 6)
mod<-lm(perc.bb~tx + species, data=percent)
display(mod)
qplot(species, perc.bb, data = percent,
geom = "boxplot", color=tx) +
xlab("Species")+ylab("Percent Budburst")
class(risk$bud)
risk$bud<-as.numeric(risk$bud)
mod1<-lm(dvr~bud+species+tx, data=risk)
display(mod)
display(mod1)
risk$frost<-ifelse(risk$bbch.first<=risk$frz, TRUE, FALSE)
mod1<-lm(dvr~bud+species+tx+frost, data=risk)
mod1<-glm(dvr~bud+species+tx+frost, data=risk)
risk$frost<-ifelse(risk$tx=="A", FALSE, risk$frost)
mod1<-glm(dvr~bud+species+tx+frost, data=risk)
display(mod1)
ggplot(risk, aes(x=bud, y=dvr, color=species)) + geom_point() + geom_abline(aes(color=species))
ggplot(risk, aes(x=bud, y=dvr, color=species)) + geom_point() + geom_smooth(method="lm")
betpop<-ggplot(risk[subset(species=="BETPOP")], aes(x=bud, y=dvr, color=tx)) + geom_point() + geom_smooth(method="lm")
betpop<-ggplot(risk[subset(risk$species=="BETPOP")], aes(x=bud, y=dvr, color=tx)) + geom_point() + geom_smooth(method="lm")
betpop<-ggplot(subset(risk$species=="BETPOP"), aes(x=bud, y=dvr, color=tx)) + geom_point() + geom_smooth(method="lm")
betpop<-subset(risk, species=="BETPOP")
bpop<-ggplot(betpop, aes(x=bud, y=dvr, color=tx)) + geom_point() + geom_smooth(method="lm")
bpop
betpap<-subset(risk, species=="BETPAP")
bpap<-ggplot(betpap, aes(x=bud, y=dvr, color=tx)) + geom_point() + geom_smooth(method="lm")
bpap
samrac<-subset(risk, species=="SAMRAC")
srac<-ggplot(samrac, aes(x=bud, y=dvr, color=tx)) + geom_point() + geom_smooth(method="lm")
srac
betula<-c("BETPOP", "BETPAP")
birch<-subset(risk, risk$species%in%betula)
mod1<-glm(dvr~bud+species+frost, data=risk)
display(mod1)
betula<-c("BETPOP", "BETPAP")
birch<-subset(risk, risk$species%in%betula)
mod2<-glm(dvr~bud+species+frost, data=birch)
display(mod2)
mod3<-glm(dvr~bud+species+tx, data=birch)
display(mod3)
mod3<-glm(dvr~bud+species+bud*tx, data=birch)
display(mod3)
mod3<-glm(dvr~species+bud:tx, data=birch)
display(mod3)
mod3<-glm(dvr~species+bud*tx, data=birch)
display(mod3)
library(gridExtra)
grid.arrange(bpop, bpap, ncol=2, nrow=1)
bpop<-ggplot(betpop, aes(x=bud, y=dvr, color=tx)) + geom_point() + geom_smooth(method="lm") +
annotate("text", x = 20, y = 80, label = "Betula populifolia", fontface = "italic")
bpap<-ggplot(betpap, aes(x=bud, y=dvr, color=tx)) + geom_point() + geom_smooth(method="lm") +
annotate("text", x = 20, y = 80, label = "Betula populifolia", fontface = "italic")
grid.arrange(bpop, bpap, ncol=2, nrow=1)
bpop<-ggplot(betpop, aes(x=bud, y=dvr, color=tx)) + geom_point() + geom_smooth(method="lm") +
annotate("text", x = 15, y = 90, label = "Betula populifolia", fontface = "italic")
bpap<-ggplot(betpap, aes(x=bud, y=dvr, color=tx)) + geom_point() + geom_smooth(method="lm") +
annotate("text", x = 20, y = 50, label = "Betula papyrifera", fontface = "italic")
grid.arrange(bpop, bpap, ncol=2, nrow=1)
bpop<-ggplot(betpop, aes(x=bud, y=dvr, color=tx)) + geom_point() + geom_smooth(method="lm") +
annotate("text", x = 20, y = 50, label = "Betula populifolia", fontface = "italic")
bpap<-ggplot(betpap, aes(x=bud, y=dvr, color=tx)) + geom_point() + geom_smooth(method="lm") +
annotate("text", x = 15, y = 90, label = "Betula papyrifera", fontface = "italic")
grid.arrange(bpop, bpap, ncol=2, nrow=1)
ggplot(birch, aes(x=bud, y=dvr, color=tx)) + geom_point() + geom_smooth(method="lm") + facet_wrap(~species)
ggplot(risk, aes(x=bud, y=dvr, color=tx)) + geom_point() + geom_smooth(method="lm") + facet_wrap(~species)
mod4<-glmer(dvr~frost+ (1|bud/species), data=birch)
mod4<-lmer(dvr~frost+ (1|bud/species), data=birch)
display(mod4)
mod4<-lmer(dvr~frost+bud+ (1|species), data=birch)
display(mod4)
mod4<-lmer(dvr~bud+species+(1|frost), data=birch)
display(mod4)
